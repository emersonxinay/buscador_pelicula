<div class="container">
  <%= form_with(model: movie) do |form| %>
    <% if movie.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

        <ul>
          <% movie.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :title %>
      <%= form.text_field :title, class:"form-control ", placeholder:"titulo" %>
    </div>

    <div>
      <%= form.label :duration %>
      <%= form.number_field :duration, class:"form-control ", placeholder:"duración" %>
    </div>

    <div>
      <%= form.label :category %>
      <%= form.text_field :category, class:"form-control ", placeholder:"categoría" %>
    </div>

    <div>
      <%= form.submit class:"btn btn-primary my-2" %>
    </div>
  <% end %>

</div>
