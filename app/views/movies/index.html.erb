<p id="notice"><%= notice %></p>
  <div class="container">
    <h1>Movies</h1>
    <p>busca tu pelicula favorita y si no esta agregala desde el boton nueva pelicula </p>
    <form class="row g-3">
    <%= search_form_for(@q) do |f| %>
      <div class="col-auto">
        
        <%= f.search_field  :title_cont, class:"form-control", placeholder:"titulo" %>
      </div>
      <div class="col-auto">
        
        <%= f.search_field  :duration_lteq, class:"form-control", placeholder:"Duración" %>
      </div>
      <div class="col-auto">
        
        <%= f.search_field  :category_eq, class:"form-control", placeholder:"categoria" %>
      </div>
      <div class="col-auto">
      
      <%= f.submit class:"form-control btn btn-primary"%>
      </div>
    <% end %>
    </form>
  <div class="table-responsive  ">
    <table class="table table-hover bg-info  table-sm my-2">
      <thead class="table-primary ">
        <tr>
          <th class="mx-4 p-2 "><%= sort_link(@q, :title) %> </th>
          <th class="mx-4 p-2 "><%= sort_link(@q, :duration) %> </th>
          <th class="mx-4 p-2 "><%= sort_link(@q, :category) %></th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody class=" text-dark">
        <% @movies.each do |movie| %>
          <tr>
            <td class="mx-4 p-2"><%= movie.title %></td>
            <td class="mx-4 p-2"><%= movie.duration %></td>
            <td class="mx-4 p-2"><%= movie.category %></td>
            <td class="mx-4 p-2"><%= link_to 'Show', movie, class:"btn btn-primary"  %></td>
            <td class="mx-4 p-2"><%= link_to 'Edit', edit_movie_path(movie), class:"btn btn-success" %></td>
            <td class="mx-4 p-2"><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
    <br>

    

    <div class="pagination">
      <div class ="pagination justify-content-center">
        <%=paginate(@movies)%>
      </div>
    </div>
  </div>
  